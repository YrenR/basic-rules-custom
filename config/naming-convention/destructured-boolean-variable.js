const destructuredVariable = {
  selector: 'variable',
  types: ['boolean'],
  modifiers: ['destructured'],
  format: null,
  filter: {
    regex: '(_(.*))',
    match: false
  }
}

module.exports = destructuredVariable
